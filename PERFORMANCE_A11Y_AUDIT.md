# Performance & accessibility audit — critical flows

Generated by `npm run audit:a11y` (scripts/audit/a11y-perf.mjs): axe-core 4.13.0 injected into a real Chromium page per flow, WCAG 2.1 A/AA rules, plus navigation timings read from the browser's own Performance API.

- Run at: 2026-09-07T05:53:32.027Z
- Target: `http://127.0.0.1:3000` (production build, demo/mock fallback — no Supabase configured)
- Flows audited: 5
- Serious/critical accessibility violations: **0**

> Measured on the CI/sandbox machine against a local server, so the timings are a **relative baseline for regressions**, not a claim about a customer's 4G phone in a shop. Field numbers come from staging on real devices — see the UAT checklist.

## Performance

| Flow | Viewport | FCP | LCP | DOMContentLoaded | Transferred | DOM nodes |
| --- | --- | --- | --- | --- | --- | --- |
| Login | 1280×900 | 108 ms | — | 80 ms | 481 KB | 304 |
| QR scan (counter) | 1280×900 | 136 ms | — | 108 ms | 418 KB | 2405 |
| Create Sale | 1280×900 | 132 ms | — | 43 ms | 418 KB | 2364 |
| Reward redemption | 1280×900 | 92 ms | — | 30 ms | 411 KB | 1343 |
| Mobile PWA (customer home) | 390×844 (touch) | 52 ms | — | 44 ms | 399 KB | 633 |

## Accessibility (axe-core, WCAG 2.1 A/AA)

| Flow | Violations | Serious/critical | Rules passed |
| --- | --- | --- | --- |
| Login | 0 | 0 | 27 |
| QR scan (counter) | 0 | 0 | 20 |
| Create Sale | 0 | 0 | 24 |
| Reward redemption | 0 | 0 | 25 |
| Mobile PWA (customer home) | 0 | 0 | 24 |

No WCAG 2.1 A/AA violations were reported on any audited flow.

## What this does and does not cover

- axe-core finds roughly a third to a half of WCAG issues. It cannot judge whether alt text is *meaningful*, whether focus order makes sense to a screen-reader user, or whether an error message is understandable. Manual keyboard and screen-reader passes are on the UAT checklist.
- Flows are audited in demo/mock fallback, because that is what runs without owner credentials. The live variants render additional panels; re-run this against staging once Supabase is configured.
- The QR-scan flow audits the counter dialog in its post-scan state; camera capture is out of scope for this MVP by design.
- **Light mode only.** This script never toggles the `.dark` class or emulates `prefers-color-scheme: dark`, so every page above was audited exactly as it renders by default (light theme). `globals.css` defines separate color tokens for `.dark` (including `--primary`, `--success` and `--warning` — the same tokens MVP_HANDOFF.md §6 records as deliberately darkened for AA contrast in light mode), and axe-core has never run against them. Dark-mode contrast is unverified, not passing — do not infer dark-mode accessibility from this report, including from the Playwright UI gate's separate "desktop light + dark" coverage, which checks functional behaviour, not WCAG contrast.

